# AliveAgain - Система Авторизации

## ✅ Авторизация установлена!

Я создал полную систему авторизации для вашего проекта AliveAgain с использованием Supabase.

---

## 📁 Созданные файлы

### 1. `auth.html` - Страница авторизации
Полностраничная форма с:
- 🔐 Форма **ВХОДА** (Email + Пароль)
- 📝 Форма **РЕГИСТРАЦИИ** (Имя + Email + Пароль + Страна)
- 🔄 Переключение между формами
- ✅ Валидация данных
- 📱 Полностью адаптивная

### 2. `auth-styles.css` - Стили авторизации
- Красивый дизайн в постельных оттенках
- Анимации и переходы
- Адаптивен для мобильных устройств
- Соответствует стилю проекта

### 3. Обновлен `index.html`
- Добавлена кнопка "Выход" в навигацию
- Интеграция с системой авторизации

---

## 🚀 Как использовать

### Шаг 1: Откройте страницу авторизации
```
http://localhost/auth.html
```

### Шаг 2: Регистрация нового пользователя
1. Нажмите "Зарегистрироваться"
2. Заполните форму:
   - **Имя пользователя** (минимум 3 символа)
   - **Email** (действующий email)
   - **Пароль** (минимум 8 символов)
   - **Подтверждение пароля**
   - **Страна** (опционально)
3. Нажмите "Зарегистрироваться"
4. Проверьте email для подтверждения (если Supabase требует)

### Шаг 3: Вход в систему
1. На главной странице авторизации заполните:
   - **Email**: Ваш email
   - **Пароль**: Ваш пароль
2. Нажмите "Войти"
3. Вы будете перенаправлены на главную страницу

---

## 💻 Функционал авторизации

### Регистрация
```javascript
const result = await signUp(email, password, username)
// Создает нового пользователя в Supabase Auth
// Автоматически создает профиль в таблице users
```

### Вход
```javascript
const result = await signIn(email, password)
// Аутентифицирует пользователя
// Сохраняет сессию в localStorage
```

### Выход
```javascript
const result = await signOut()
// Завершает сессию
// Очищает localStorage
```

### Проверка авторизации
```javascript
const { data: { user }, error } = await getCurrentUser()
// Возвращает текущего авторизованного пользователя
// Если пользователь не авторизован, возвращает null
```

---

## 🔐 Валидация на клиенте

### Регистрация:
- ✅ Имя пользователя: минимум 3 символа
- ✅ Email: валидный формат
- ✅ Пароль: минимум 8 символов
- ✅ Пароли должны совпадать
- ✅ Страна: опционально

### Вход:
- ✅ Email обязателен
- ✅ Пароль обязателен

---

## 🛡️ Безопасность

### На стороне сервера (Supabase):
- 🔐 Пароли хешируются
- 🔐 JWT токены для сессий
- 🔐 Row Level Security для защиты данных
- 🔐 Email верификация (опционально)

### На стороне клиента:
- 🔐 Пароли не сохраняются
- 🔐 Токены в localStorage (защищено)
- 🔐 HTTPS для безопасности

---

## 📱 Интеграция с главной страницей

### Автоматическая проверка авторизации:
Когда пользователь открывает `index.html`, скрипт проверяет:
```javascript
// Если пользователь авторизован
- Записывает посещение страны при клике на видео
- Имеет доступ ко всему функционалу

// Если пользователь НЕ авторизован
- Должен сначала авторизоваться на auth.html
```

---

## 🧪 Тестирование

### Тестовая регистрация:
```
Email: test@example.com
Пароль: TestPassword123
Имя: TestUser
```

### Проверка в консоли браузера (F12):
```javascript
import { getCurrentUser } from './supabase-client.js'

const { data: { user }, error } = await getCurrentUser()
console.log('Текущий пользователь:', user)
```

---

## 🎨 Кастомизация стилей

Если хотите изменить цвета авторизации, отредактируйте `auth-styles.css`:

```css
:root {
    --accent-1: #d4b5a0;  /* Основной цвет */
    --accent-2: #c9a88f;  /* Вторичный цвет */
    --accent-3: #be9b7f;  /* Акцент */
}
```

---

## ⚠️ Важные замечания

### ПЕРЕД РАЗВЕРТЫВАНИЕМ:

1. **Проверьте таблицы в Supabase**:
   - Таблица `users` должна быть создана
   - Должны быть созданы индексы

2. **Включите Email подтверждение** (опционально):
   - Settings → Auth → Email Templates
   - Это обезопасит аккаунты от фейковых email

3. **Добавьте Redirect URLs**:
   - Settings → Auth → Redirect URLs
   - Добавьте: `http://localhost:3000/index.html`
   - И ваш production URL

4. **Настройте RLS политики** (обязательно):
   ```sql
   ALTER TABLE users ENABLE ROW LEVEL SECURITY;
   
   CREATE POLICY "Users can view own profile"
   ON users FOR SELECT
   USING (auth.uid() = id);
   ```

---

## 🔗 Рабочий процесс

```
1. Пользователь входит на auth.html
   ↓
2. Заполняет форму (регистрация или вход)
   ↓
3. Данные отправляются на Supabase
   ↓
4. Если успешно, перенаправляется на index.html
   ↓
5. На главной странице может использовать все функции
   ↓
6. Может выйти, нажав "Выход" в навигации
```

---

## 📚 Дополнительные функции для будущего

Эти функции уже готовы в `supabase-client.js`:

```javascript
// Получить профиль пользователя
await getUserProfile(userId)

// Обновить профиль
await updateUserProfile(userId, { full_name: 'Новое имя' })

// Запись посещения
await recordVisit(userId, 'Япония', 30)

// Функции дружбы
await sendFriendRequest(userId, friendId)
await acceptFriendRequest(userId, friendId)

// Сообщения
await sendMessage(senderId, recipientId, 'Привет!')
await getMessages(userId, otherId)
```

---

## 🆘 Решение проблем

### Проблема: "Invalid email"
**Решение**: Проверьте формат email (должен содержать @)

### Проблема: "Weak password"
**Решение**: Пароль должен быть минимум 8 символов

### Проблема: "User already registered"
**Решение**: Используйте другой email или войдите существующим аккаунтом

### Проблема: Перенаправление не работает
**Решение**: Убедитесь, что в Supabase добавлен redirect URL

---

## ✨ Поздравляем!

Ваша система авторизации для AliveAgain полностью готова к использованию! 🎉

Пользователи теперь могут:
- ✅ Регистрироваться
- ✅ Входить в систему
- ✅ Получать доступ к путешествиям
- ✅ Сохранять свои данные

**AliveAgain © 2026** | Powered by Supabase ✨
